# 热念 v2 权限与状态

## 身份与关系

renianApi/index.js 只使用 getWXContext().OPENID，并强制 apiVersion 为 2。业务错误返回明确错误码，未知异常不返回数据库详情。

用户文档使用 OPENID 为 ID。每次业务读写核验 v2_users.coupleId、用户状态、v2_couples.status 和双人成员集合。请求中的 coupleId、authorOpenid 不参与身份判断。用户视图不返回双方 OPENID。

邀请以随机 8 位码为 v2_invites 文档 ID，生成、刷新、加入、取消在事务内处理。已建立的关系不能通过取消邀请解除。加入失败不影响自己的原邀请。

所有 v2 数据集合拒绝客户端直接读写，由云函数管理员上下文访问。

## 写入幂等与冲突

日常、约定、心意券、图片准备使用 requestId；v2_operations 以操作、调用者、requestId 的哈希为 ID，事务保存输入指纹和结果。同一 ID 同一内容重试只返回已有结果；换内容重试返回 REQUEST_CONFLICT。

修改和状态切换同时校验 expectedVersion，旧版本不能覆盖新状态。分页用 createdAt 和文档 ID 的稳定游标，游标绑定关系和过滤条件。

## 可见性和操作角色

- 日常发布后即对本关系双方可见，仅作者可编辑和删除。
- 约定提议由另一方接受或拒绝；发起人可撤回。修改被接受时，旧版替换与新版生效在同一事务完成。
- 心意券赠送方不能替收到的人申请；收到的人不能批准自己的申请。申请与结果单独保存历史。
- 当前心情仅本人可更新，对方只读。

## 图片

media.prepare 返回随机暂存路径，先登记 storage fileId，防止上传后未确认造成无法追踪的临时文件。正式确认要求文件属于当前环境、此次准备路径及当前用户。

存储规则仅允许创建者写入 v2-upload/ 暂存文件；其他路径不可由客户端写入，直接读取关闭。服务端验证文件头、尺寸上限，复制到 v2-published/，该文件客户端不能覆盖。只允许本人未发布图片或当前关系已发布图片获取临时链接。

确认过程持有 5 分钟租约；清理任务不会删除租约内的暂存文件。未使用图片 24 小时后可清理，删除记录和移除图片进入 cleanup_pending。删除失败保持可重试状态。数据库过期不等于文件已删除。

临时链接有有效期，已拿到的链接在有效期内可继续访问；不要将链接输出到日志。逻辑删除立即从列表、统计和新授权中移除，物理文件由清理任务删除。

## 提醒

开启提醒要求用户主动操作订阅。服务端仅存储开关和时间，不假定能替用户授予微信订阅权限。默认关闭，设置带版本号防止旧页面覆盖。

发送任务事务领取当天任务，发送前再次检查分享状态、关系和设置版本；成功后消耗本次提醒授权。外部消息发送不能加入数据库事务，最后一次检查与微信发送之间仍存在极短的竞争窗口。发送结果不确定时不自动重发，避免重复通知。

dailyReminder 和 mediaCleanup 不提供业务客户端入口，拒绝带 OPENID 的直接调用；部署时应同时禁止普通客户端调用这两个调度函数，仅保留定时触发和管理员执行。
